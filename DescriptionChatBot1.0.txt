A chatterbot is a computer program that responds to user queries by replying in the same language(like English). A very basic chatterbot can be written in a few lines of code in a given specific programming language. All the code for this project will be written in C++. This is the first iteration of my attempt to create a chatter bot. It answers very basic queries which is already known to it. As of now its questions are case-sensitive